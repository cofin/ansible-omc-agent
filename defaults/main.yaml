---
# defaults file for omc cloudagent configuration


### Constants
omc_agent:
  user: oracle
  uid: 1001
  gid: 1001
  group: oinstall
  users_home: /home/oracle
  # version
  version: 1.40.0
  omc_url:
  gateway_host:
  gateway_port:
  tenant_name:
  registration_key:
  base_dir: /opt/omc/agent
  additional_gateways:
  packages:
    - bc
    - setarch
    - jq